<%# locals: (thread:, children_index:, selected_id:) %>
<li class="weave-thread-node-wrap prompt-weave-node-wrap" role="none">
  <button
    type="button"
    class="weave-thread-select prompt-weave-thread-select<%= " is-selected" if selected_id == thread.public_id %>"
    data-thread-id="<%= thread.public_id %>"
    data-action="weave-panel#select"
    role="treeitem"
    aria-pressed="<%= selected_id == thread.public_id %>"
  >
    <span class="weave-thread-select-label prompt-weave-thread-label"><%= thread.title.to_s.truncate(52) %></span>
  </button>
  <% children = children_index[thread.id] %>
  <% if children.present? %>
    <ul class="weave-thread-children prompt-weave-thread-children" role="group">
      <% children.each do |child| %>
        <%= render "sequences/weave_thread_tree_node", thread: child, children_index: children_index, selected_id: selected_id %>
      <% end %>
    </ul>
  <% end %>
</li>
